import {NextResponse} from "next/server";import {byId} from "../../../lib/db";import {products,ProductKey} from "../../../lib/products";
export async function GET(req:Request){const id=new URL(req.url).searchParams.get("id");if(!id)return NextResponse.json({error:"Pedido não informado."},{status:400});const o=byId(id);if(!o)return NextResponse.json({error:"Pedido não encontrado."},{status:404});const p=products[o.productKey as ProductKey];return NextResponse.json({id:o.id,status:o.status,approved:o.status==="approved"&&o.delivered,product:p?{name:p.name,deliveryUrl:p.deliveryUrl}:null})}
